# Aplicaciones

Las aplicaciones ejecutables viven bajo `apps/`. Cada servicio mantiene su
contrato, adaptadores, pruebas y documentación localmente para evitar que el
entrypoint dependa de archivos dispersos en la raíz.
